// Auto-generated by CMake from composite.vert.dxil, composite.frag.dxil and
// cursor.frag.dxil. DO NOT EDIT — regenerate by re-running CMake configure
// after updating the .dxil files. See the README in this directory for the dxc
// invocation that produces the .dxil files.

#pragma once

#include <cstddef>
#include <cstdint>

namespace fheroes2
{
    namespace shaders
    {
        // Shared vertex shader (parametric quad emitted from SV_VertexID, used by
        // both the framebuffer composite and the cursor overlay).
        inline constexpr uint8_t composite_vert_dxil[] = { @COMPOSITE_VERT_C_ARRAY@ };

        // Phase 3 framebuffer composite: indexed > 0 → palette[idx], else RGBA.
        // Three sampled textures: indexedTex (R8), rgbaTex (RGBA8), paletteTex (RGBA8 256x1).
        inline constexpr uint8_t composite_frag_dxil[] = { @COMPOSITE_FRAG_C_ARRAY@ };

        // Plain RGBA pass-through used for the software cursor overlay.
        inline constexpr uint8_t cursor_frag_dxil[] = { @CURSOR_FRAG_C_ARRAY@ };

        inline constexpr size_t composite_vert_dxil_size = sizeof( composite_vert_dxil );
        inline constexpr size_t composite_frag_dxil_size = sizeof( composite_frag_dxil );
        inline constexpr size_t cursor_frag_dxil_size = sizeof( cursor_frag_dxil );
    }
}
