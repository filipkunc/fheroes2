// Auto-generated by CMake from the .dxil and .spv binaries in this directory.
// DO NOT EDIT - regenerate by re-running CMake configure after updating the
// shader binaries. See the README in this directory for the dxc / glslang
// invocations that produce the binaries.

#pragma once

#include <cstddef>
#include <cstdint>

namespace fheroes2
{
    namespace shaders
    {
        // ----- DXIL bytecode (D3D12 backend, Windows) -----
        inline constexpr uint8_t composite_vert_dxil[] = { @COMPOSITE_VERT_DXIL_C_ARRAY@ };
        inline constexpr uint8_t composite_frag_dxil[] = { @COMPOSITE_FRAG_DXIL_C_ARRAY@ };
        inline constexpr uint8_t cursor_frag_dxil[] = { @CURSOR_FRAG_DXIL_C_ARRAY@ };

        inline constexpr size_t composite_vert_dxil_size = sizeof( composite_vert_dxil );
        inline constexpr size_t composite_frag_dxil_size = sizeof( composite_frag_dxil );
        inline constexpr size_t cursor_frag_dxil_size = sizeof( cursor_frag_dxil );

        // ----- SPIR-V bytecode (Vulkan backend, Linux / Android) -----
        inline constexpr uint8_t composite_vert_spv[] = { @COMPOSITE_VERT_SPV_C_ARRAY@ };
        inline constexpr uint8_t composite_frag_spv[] = { @COMPOSITE_FRAG_SPV_C_ARRAY@ };
        inline constexpr uint8_t cursor_frag_spv[] = { @CURSOR_FRAG_SPV_C_ARRAY@ };

        inline constexpr size_t composite_vert_spv_size = sizeof( composite_vert_spv );
        inline constexpr size_t composite_frag_spv_size = sizeof( composite_frag_spv );
        inline constexpr size_t cursor_frag_spv_size = sizeof( cursor_frag_spv );
    }
}
